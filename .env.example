# =============================================================================
# Grunge Pallets - Environment Template
# Copy to `.env.local` and fill values. Do NOT commit `.env.local`.
# =============================================================================

# -----------------------------------------------------------------------------
# App URL (used for links inside emails; strongly recommended in production)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# -----------------------------------------------------------------------------
# Supabase (required)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Required for admin operations + Supabase Storage uploads (service role key)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# -----------------------------------------------------------------------------
# Email (Resend) - transactional emails from the app
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=Grunge Pallets <no-reply@grungepallets.com>

# Used for admin notifications (new user, new order, new pickup, contact/quote)
ADMIN_EMAIL=ops@grungepallets.com

# Logo used by app-sent emails. Defaults to https://grungepallets.com/logo.jpg if unset.
EMAIL_LOGO_URL=https://grungepallets.com/logo.jpg

# -----------------------------------------------------------------------------
# Forms -> n8n webhooks (optional, server-side forwarding)
# Keep these NOT public (do not use NEXT_PUBLIC_*)
# -----------------------------------------------------------------------------
N8N_WEBHOOK_SECRET=your-shared-hmac-secret
N8N_QUOTE_WEBHOOK_URL=https://n8n.example.com/webhook/forms/quote
N8N_PICKUP_WEBHOOK_URL=https://n8n.example.com/webhook/forms/pickup
N8N_CONTACT_WEBHOOK_URL=https://n8n.example.com/webhook/forms/contact

# Optional privacy / intake toggles
FORMS_INCLUDE_IP=false

# Pickup photos: store intake photos in Supabase Storage and forward URLs/paths to n8n.
FORMS_STORE_PICKUP_PHOTOS_IN_SUPABASE=true

# Required if FORMS_STORE_PICKUP_PHOTOS_IN_SUPABASE=true
# Create this bucket in Supabase Storage and mark it public (for easy n8n download),
# then schedule cleanup (e.g. n8n cron) to delete objects older than your retention window.
SUPABASE_INTAKE_BUCKET=intake-temp
